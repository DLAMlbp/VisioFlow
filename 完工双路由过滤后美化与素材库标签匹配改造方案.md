# 完工双路由过滤后美化与素材库标签匹配改造方案

## 1. 文档结论

本次改造的唯一目标流水线为：

```text
上传图片
  -> 本地技术预检
  -> AI 完工/非完工分类
  -> 后端选择唯一过滤分支
       completed    -> 完工过滤标准
       construction -> 非完工过滤标准
       insufficient_evidence / invalid_or_irrelevant -> 直接拒绝并记录原因
  -> 等待本批全部图片完成过滤
  -> 对过滤通过的图片执行统一美化标准
       AI 只规划逐图美化参数
       本地策略校验参数
       OpenCV/Pillow 执行像素处理
  -> 从美化后图片生成 OpenCLIP 向量
  -> 只按图片向量匹配素材库参考图片
  -> 匹配成功后只继承素材组人工标签
  -> 输出结果
```

必须同时满足以下硬约束：

1. 完工分类、分支过滤、美化规划是三个独立 AI 子阶段，均使用已配置的 AI API Key。
2. 分支过滤请求不得再返回美化参数和内容标签。
3. 一批图片未全部完成过滤前，任何图片都不得开始美化规划或像素美化。
4. 完工和施工图片使用同一份任务级美化标准快照，但每张图片独立规划参数。
5. 美化后的图片是 OpenCLIP 查询向量的唯一图像来源。
6. 素材匹配不得使用大模型内容特征，最终相似分只等于图片向量相似度。
7. 最终标签只能来自 `library_asset_groups.tags`，不能合并、补充或回退到大模型标签。
8. 未匹配或待人工复核时，最终标签必须为空；候选标签只能出现在复核候选中。
9. 历史任务继续可读，新任务不得进入旧的 AI 自动标签路径。
10. 任一阶段失败必须可重试、可恢复、不可重复计数，也不可越过前置阶段。

## 2. 当前实现审计

当前代码已经具备一部分基础能力，但尚未完整符合目标业务。

| 能力 | 当前状态 | 结论 |
| --- | --- | --- |
| 单图完工分类 | 已有 `src/workers/completion.py` | 保留并加强测试 |
| 后端双分支路由 | 已有 `src/workers/processing.py` | 保留，调整输出协议 |
| 三套标准快照 | 已加入模型、Schema 和迁移草稿 | 完成迁移验证 |
| 本地技术预检 | 已有 `src/workers/preprocess.py` | 保留 |
| 整批过滤屏障 | 仓储层有未使用的方法，但主路径会逐图直接投递美化 | 必须修复 |
| 分支过滤请求 | 同时返回 `filter`、`beautify`、`content` | 必须拆分 |
| 美化后内容分析 | 实际复用美化前的 `content`，没有分析美化图 | 删除活动路径 |
| 素材库目标图向量 | 已从 `analysis_object_key` 生成 OpenCLIP 向量 | 保留并校验来源 |
| 素材库参考图入库 | 仍调用大模型做内容分析后才生成向量 | 改为只做本地向量 |
| 相似度 | 70% 图片向量 + 30% 大模型内容特征 | 改为 100% 图片向量 |
| 最终标签 | 合并素材库标签和大模型识别标签 | 改为只取素材组标签 |
| 未匹配行为 | 仍可能保留大模型标签 | 改为空标签 |
| 前端文案 | 仍有“AI 自动标签”“AI 内容标签” | 改为“素材库匹配标签” |

当前最危险的行为位于 `src/workers/preprocess.py::_advance_after_preprocess`：当 `max_selected >= total_count` 时，单张图片一旦过滤通过就立即投递 `image.enhance`。前端当前把 `max_selected` 设置为上传总数，因此实际运行不是“整批过滤完成后再美化”。

## 3. 业务标准

### 3.1 完工分类标准

完工分类只回答“装修状态是什么”，不回答“这张图是否值得保留”。外部只返回：

```text
completed
non_completed
```

内部保留四个 subtype：

```text
completed
construction
insufficient_evidence
invalid_or_irrelevant
```

只有以下条件全部满足才能归一化为 `completed`：

1. 真实、有效、可判断主要室内空间。
2. 可见范围已脱离明显施工状态。
3. 墙、地、顶及主要基础硬装基本完成。
4. 存在明确成品空间或固定装修成果。
5. 整体已具备使用、接近使用或展示条件。

以下情况不能单独推翻完工：家具未全部进场、缺少部分家电或软装、少量保护膜和包装箱、少量清理痕迹、个别不影响使用的小部件收尾。

以下任一情况必须为 `non_completed`：明显施工或毛坯、基础硬装大面积未完成、完工证据不足、严重模糊或遮挡、效果图/图纸/商品图/室外图/无关图片。

服务端必须根据事实字段归一化结果，不能直接信任模型给出的标签。模型把事实不完整的图片输出为 `completed` 时，服务端必须改为 `non_completed`。

### 3.2 完工过滤标准（可直接录入标准管理）

```text
本标准只处理后端已经判定为 completed 的真实室内装修照片，不再重新判断完工类别。

保留条件：
1. 画面能够清楚展示完整空间、主要区域或有辨识价值的完工装修成果；
2. 墙面、地面、顶面、固定柜体、厨卫设施、背景墙、门窗收口等主体内容可辨认；
3. 画面具有真实完工记录或成品展示价值；
4. 少量保护膜、包装箱、清理痕迹、家具或软装未完全进场可以保留；
5. 不得仅因缺少家具、家电、窗帘、绿植或装饰品而拒绝。

拒绝条件：
1. 拍摄范围过小，只能看到无上下文的单个产品、材料或局部构件；
2. 主体被严重遮挡，无法看清装修成果；
3. 图片虽被分类为完工，但当前画面没有足够的成品空间展示价值；
4. 发现与完工事实明显冲突的大面积施工区域时拒绝，并记录分类冲突供人工复核；
5. 非真实室内照片、效果图、图纸、商品图、室外图或无关图片一律拒绝。

尺寸、文件损坏、重复、严重清晰度和曝光问题由本地技术预检负责，本标准不重复编造数值阈值。
```

### 3.3 非完工过滤标准（可直接录入标准管理）

```text
本标准只处理后端已经判定为 construction 的真实室内施工照片。施工中、材料工具、裸露管线和未完成表面本身不是拒绝理由。

保留条件：
1. 图片是真实室内装修施工记录；
2. 能够看出空间、施工阶段、施工节点、工艺过程或未完成部位中的至少一项；
3. 毛坯、水电、泥木、瓦工、油漆、吊顶、柜体、洁具、设备安装或收尾阶段均可保留；
4. 画面中的材料、工具、垃圾、保护措施、裸露管线或未完成区域能够证明施工状态时可以保留；
5. 局部施工节点可以保留，但必须有足够上下文证明它属于真实装修现场，而不是商品特写。

拒绝条件：
1. 仅有单个建材、家具、家电、工具或包装的商品式特写，无法确认施工现场；
2. 范围过小、严重模糊、过暗或遮挡，无法识别空间和施工内容；
3. 效果图、户型图、施工图、设计图纸、室外图或无关图片；
4. 重复图片、损坏图片或不满足本地技术预检的图片；
5. 没有施工记录价值、没有空间上下文，也没有可辨认工艺节点的图片。

不得套用完工图的“整洁、无材料、无工具、成品化”要求，否则会错误过滤掉正常施工记录。
```

`insufficient_evidence` 和 `invalid_or_irrelevant` 对外仍显示“非完工”，但不进入非完工 AI 过滤调用，直接按明确原因拒绝。这样既符合二分类，又避免无效图片被施工分支误放行。

### 3.4 通用美化标准

所有分支过滤通过的图片使用同一份任务级标准快照：

```text
1. 适度提升整体亮度并打开暗部，禁止过曝发白和丢失材质细节；
2. 微调对比度和局部层次，改善灰蒙和发闷，不制造生硬高反差；
3. 校正黄雾、灰雾和白平衡，还原红砖、水泥、木材和墙地面真实颜色；
4. 适度降噪并增强真实纹理，禁止过度锐化、白边和伪影；
5. 完整保留原图内容和物体位置，不增加、删除、替换或移动任何内容；
6. 不裁切、不拉伸、不 AI 扩图，不改变空间比例；
7. 输出 sRGB JPEG；如业务要求 3:4，则非 3:4 输入在预检阶段拒绝或进入复核，不能靠裁切修正。
```

同一标准不等于所有图片使用相同参数。AI 根据每张过滤通过的图片单独返回参数，本地校验器将参数限制在标准允许范围内。

## 4. 目标状态机

### 4.1 图片状态

建议新任务采用以下状态：

```text
queued
  -> analyzing              本地预检、完工分类、分支过滤
  -> filtered               分支过滤通过，等待批次屏障
  -> beautify_planning      批次屏障通过后规划美化参数
  -> enhancing              本地执行美化
  -> enhanced               美化完成
  -> matching               生成向量并匹配素材库
  -> selected               结果完成
```

过滤阶段可直接进入的终态：

```text
rejected
failed
cancelled
```

质量优选开启时，`filtered` 还可在批次排序后进入 `not_selected`。默认 `max_selected=上传数量`，即美化全部过滤通过图片；无论是否优选，都必须先经过批次屏障。

### 4.2 批次过滤屏障

批次进入下一阶段的充分必要条件：

```text
该 job 下不存在 status in ('queued', 'analyzing') 的图片
```

实现要求：

1. 删除 `_advance_after_preprocess` 中 `max_selected >= total_count` 时逐图直接发布美化任务的分支。
2. 所有过滤成功、拒绝、分类拒绝和过滤失败路径最后统一调用 `maybe_start_post_filter_phase(job_id)`。
3. 仓储层使用带 Job 状态条件的原子 `UPDATE ... RETURNING` 抢占屏障，只有一个 Worker 能成功发布后续任务。
4. 成功抢占后统一发布 `image.rank_job`；排序任务再发布美化规划任务，而不是直接发布像素美化任务。
5. 定时恢复任务应扫描“已无待过滤图片但 Job 仍停在过滤状态”的任务，并重新触发屏障。
6. 屏障查询和 Job 状态更新必须在同一事务逻辑内，重复消息不得造成重复投递和重复计数。

### 4.3 阶段失败策略

| 阶段 | 失败处理 |
| --- | --- |
| 本地预检 | 单图拒绝或失败，继续推进批次屏障 |
| 完工分类 | 可重试错误重试；最终失败为单图 failed，不得默认归为非完工 |
| 分支过滤 | 可重试错误重试；最终失败为单图 failed，不得默认 pass |
| 美化规划 | 重试后失败为单图 failed，不得使用固定参数静默继续 |
| 本地美化 | 幂等重试；最终失败为单图 failed |
| OpenCLIP 向量 | 图片仍可交付，但匹配为 unmatched、标签为空并记录错误 |
| 素材库为空 | 图片正常交付，匹配为 unmatched、标签为空 |
| 待人工复核 | 图片正常交付，最终标签为空，候选只进入复核队列 |

## 5. AI 调用边界

### 5.1 必须使用 API Key 的阶段

以下阶段需要统一读取并校验 AI API Key：

1. 完工状态分类；
2. 已路由分支的业务过滤；
3. 过滤通过后的逐图美化参数规划。

创建任务前的预检接口必须验证：AI 已启用、API Key 非空、模型和 Base URL 可用。缺失时前端阻止提交并显示明确错误，后端也必须再次校验，不能退回本地猜测或静默放行。

### 5.2 不得使用大模型的阶段

以下阶段不得调用大模型：

1. 图片像素美化执行；
2. 美化后查询向量生成；
3. 素材库参考图片入库和向量生成；
4. 素材库相似匹配；
5. 最终标签生成或补全。

这些阶段只允许本地图片算法、OpenCLIP、向量数据库和人工素材组标签。

### 5.3 三个严格响应协议

完工分类响应只包含事实、类别、置信度、原因码和理由。

分支过滤响应只包含：

```json
{
  "filter": {
    "decision": "pass",
    "reason": "",
    "confidence": 0.0,
    "dimensions": [
      {"dimension": "", "passed": true, "reason": ""}
    ]
  }
}
```

美化规划响应只包含：

```json
{
  "needed": true,
  "reason": "",
  "confidence": 0.0,
  "parameters": {},
  "parameter_reasons": {},
  "risk_flags": []
}
```

三个协议均不得出现 `content`、`tags`、`categories` 或 `candidate_tags`。

## 6. 美化阶段改造

### 6.1 独立美化规划任务

新增：

```text
src/workers/beautify_plan.py
image.plan_beautify
beautify_plan 队列
BeautifyPlanTaskPublisher
```

输入必须包含：

- 美化标准版本快照；
- 当前原图或方向校正后的高细节预览；
- 宽高、比例和本地质量指标；
- 当前参数边界和禁止操作。

输出保存原始计划和经本地策略修正后的执行参数。`image.enhance` 只能 claim `beautify_plan_status=completed` 的图片。

### 6.2 数据迁移

在现有双路由迁移之后新增一条 Alembic 迁移，向 `image_items` 增加：

```text
beautify_plan_status
beautify_plan_json
beautify_plan_model
beautify_plan_prompt_version
beautify_plan_duration_ms
beautify_plan_error
beautify_plan_started_at
beautify_plan_completed_at
```

不删除 `ai_processing_json`，但新任务只在其中保存分支过滤结果。历史数据继续按旧结构读取。

不重命名 `image_ai_tags`，避免破坏历史兼容。新任务将该表作为“标签结果兼容存储”，写入时必须使用：

```text
provider = library
model_name = 当前 OpenCLIP embedding_version
prompt_version = 当前 similarity profile 版本
tag_json.tags = 最终素材库标签或空数组
tag_json.categories = {"素材库标签": [...]} 或空对象
tag_json.candidate_tags = []
```

`analysis_status` 和 `feature_score` 保留用于读取历史任务；新任务不依赖它们。后续大版本再考虑无损重命名，不在本次上线中做破坏性迁移。

### 6.3 本地执行与验收

`src/services/images/beautify_policy.py` 负责字段完整性、系统安全范围、标准动态范围、参数冲突和禁止操作校验。

`src/workers/enhance.py` 负责：

```text
EXIF 方向校正
  -> 参数策略校验
  -> 小图预演
  -> 过曝/色偏/噪声/锐化白边检查
  -> 最多一次确定性参数修正
  -> 正式图处理
  -> 最终质量验收
  -> 上传美化图和分析缩略图
```

必须记录：标准快照、AI 原始参数、最终执行参数、策略修正、处理前后指标、验收状态和回退原因。

## 7. 素材库匹配与标签契约

### 7.1 素材库参考图片入库

修改 `src/workers/library.py`：

1. 删除 `get_tag_provider(...).tag(...)` 调用；
2. 只执行解码、方向校正、去重、缩略图和 OpenCLIP embedding；
3. `analysis_json` 对新素材保持空值；
4. 素材可启用条件改为 `embedding != null`，不再要求 `analysis_json`；
5. 素材标签只由用户在素材组中人工维护。

### 7.2 目标图片查询向量

查询向量必须使用 `enhanced_object_key` 对应的美化后图片生成。为控制成本可以从美化后图片生成等比例分析缩略图，但必须保证：

```text
image_ai_tags.source_object_key
image_items.analysis_object_key
embedding 实际下载 key
```

都能追溯到同一张美化结果，不得回退到原图或分支过滤时的预览图。

### 7.3 纯图片相似度

将相似度配置改为：

```yaml
similarity_image_weight: 1.0
similarity_feature_weight: 0.0
```

代码中移除 `feature_similarity()` 对新任务的调用。新记录：

```text
similarity_score = OpenCLIP cosine similarity
feature_score = null
final_score = similarity_score
```

不能只把 `feature_score` 设为 0 而保留 0.70/0.30 权重。那会把 0.60 自动阈值等价提高为图片相似度约 0.857，导致大量正常图片无法匹配。

### 7.4 三段式决策

匹配策略必须完整使用自动阈值、复核阈值和第一第二名差值：

```text
best_score >= auto_threshold 且 margin >= min_margin
  -> matched
  -> matched_asset_id = 第一名素材
  -> final tags = matched_asset.group.tags 快照

review_threshold <= best_score < auto_threshold
或 best_score 足够但 margin 不足
  -> pending_review
  -> matched_asset_id 可作为候选保存
  -> final tags = []
  -> candidate_json 保存候选素材和其素材组标签

best_score < review_threshold 或没有候选
  -> unmatched
  -> matched_asset_id = null
  -> final tags = []
```

人工确认 `pending_review` 后，才把被确认素材当前所属组的人工标签复制到 `matched_tags_snapshot`。人工标记未匹配后，标签保持为空。

### 7.5 单一标签来源

结果写入必须满足以下不变量：

```text
decision == matched
  => result.tags == image_similarity_matches.matched_tags_snapshot
  => result.tags == 确认时 matched_asset.group.tags 的快照

decision != matched
  => result.tags == []
```

禁止以下行为：

- 合并 `recognized_tags`；
- 读取分支过滤响应中的内容标签；
- 在无匹配时保留旧模型标签；
- 把候选标签显示为已确认标签；
- 由大模型根据图片自由生成新标签。

## 8. 后端文件级修改清单

| 文件 | 必须修改内容 |
| --- | --- |
| `src/services/images/processing_vision.py` | 删除 `content` 和 `beautify`，改为分支过滤专用协议及提示词 |
| `src/workers/processing.py` | 只执行路由过滤，不读取美化标准，不生成内容标签 |
| `src/services/images/beautify_planning.py` | 新增美化规划协议和 AI 服务 |
| `src/workers/beautify_plan.py` | 新增过滤后逐图美化规划任务 |
| `src/services/jobs/dispatch.py` | 新增美化规划发布器；停止活动路径发布 AnalysisTaskPublisher |
| `src/workers/preprocess.py` | 删除逐图直达美化的捷径，所有路径进入统一批次屏障 |
| `src/workers/control.py` | 屏障后排序并发布美化规划任务 |
| `src/repositories/jobs.py` | 新增美化规划 claim/save/retry；修改屏障和匹配 readiness |
| `src/workers/enhance.py` | 读取独立美化计划；完成后只发布 embedding，不发布 analysis |
| `src/workers/analysis.py` | 从新任务活动路径移除，仅保留历史兼容或最终删除任务注册 |
| `src/workers/matching.py` | 只依赖 embedding；删除内容特征相似度和标签合并 |
| `src/services/images/similarity.py` | 实现纯图片三段式决策，pending_review 最终标签为空 |
| `src/workers/library.py` | 素材入库删除大模型内容分析，只生成本地 embedding |
| `src/services/library.py` | 素材启用条件只要求 embedding；人工复核同步素材组标签快照 |
| `src/workers/cleanup.py` | 增加屏障和美化规划恢复；移除新任务 analysis 恢复 |
| `src/workers/celery_app.py` | 注册美化规划任务 |
| `src/core/config.py` | 增加美化规划重试、超时、队列和功能开关配置 |
| `src/models/image_item.py` | 增加美化规划审计字段 |
| `src/schemas/jobs.py` | 增加新状态和素材库标签响应；旧 ai_tags 标记兼容 |
| `src/services/jobs/service.py` | 新任务只从 similarity match 映射最终标签 |
| `profiles/tags/library_similarity_v2.yaml` | 权重改为 1.0/0.0，版本递增，阈值重新校准 |
| `docker-compose.yml` | 增加 `beautify_plan` Worker 或让独立 vision Worker 监听该队列 |
| `docker-compose.prod.yml` | 生产队列隔离、并发、prefetch 和健康检查 |
| Alembic 新迁移 | 新增美化规划审计字段和索引，不破坏历史列 |

还需检查并清理旧的 `src/workers/tagging.py`。如果它已不在 Celery 注册和任何发布路径中，可保留一个版本后删除；如果仍可被调用，必须加新任务模式保护，禁止它写入最终标签。

## 9. API 与前端改造

### 9.1 API

新增清晰的标签响应，建议命名为：

```json
{
  "library_tags": {
    "source": "library",
    "decision": "matched",
    "tags": ["客厅", "现代风格", "完工"],
    "matched_asset_id": "ast_xxx",
    "similarity": 0.82,
    "message": "已匹配到相似图片素材"
  }
}
```

兼容期继续返回 `ai_tags`，但新任务中的值只能镜像已确认素材库标签，`provider=library`。前端不得把兼容字段解释为大模型生成标签。

API 必须分别返回：

- 完工分类结果和理由；
- 实际路由过滤标准及审核维度；
- 美化计划和执行验收摘要；
- 素材匹配决策、相似度、最终标签；
- 待复核候选（仅复核接口返回）。

### 9.2 前端

必须修改：

1. 流水线文案改为“完工分类 -> 分支过滤 -> 整批过滤完成 -> 统一美化 -> 素材库匹配 -> 继承标签”。
2. “AI 自动标签”“AI 内容标签”全部改为“素材库匹配标签”。
3. 已匹配标签只读取 `library_tags.tags`。
4. `pending_review` 显示“待人工确认”，但正式标签区域为空。
5. 候选素材组只在复核面板显示，不得提前显示成正式标签。
6. 未匹配显示“未匹配到素材，暂无标签”，不得显示大模型回退标签。
7. 素材库说明改为“参考图片只做校验、去重和本地向量生成”。
8. 任务进度中的 `content_analysis` 改为“素材匹配”，增加“美化规划”阶段。
9. 继续展示完工/非完工分类、实际分支、过滤理由和复核标记。

主要文件：

```text
frontend/src/App.tsx
frontend/src/LibraryWorkspace.tsx
frontend/src/types.ts
frontend/src/services/api.ts
frontend/src/utils/decision.ts
```

## 10. 阈值标定

不能直接凭经验把现有 0.60 当作纯图片阈值上线。实施时建立人工标注集：

- 至少 300 组“应匹配”查询图和素材图；
- 至少 300 组“相似但不应匹配”；
- 覆盖完工与施工、不同房间、视角、亮度和美化强度；
- 记录正确素材组、可接受的多个参考素材、应 unmatched 的样本。

标定步骤：

1. 对所有美化后查询图和素材图重新生成同一版本 OpenCLIP 向量。
2. 输出 Top-1、Top-2、Top-5 分数和 margin。
3. 选择能使自动匹配 precision 达到至少 97% 的 `auto_threshold` 和 `min_margin`。
4. 选择复核召回可接受的 `review_threshold`。
5. 首次灰度只产出 shadow 决策，不自动继承标签。
6. 人工验证达标后再开启自动 matched。

OpenCLIP 模型版本变化时，必须重建全部素材向量并重新标定阈值，禁止混用不同 embedding 版本。

## 11. 幂等、恢复与一致性

每个 Worker 必须通过条件更新 claim：

```text
pending -> processing -> completed/failed
```

必须覆盖以下中断点：

- 分类保存成功但路由消息未发送；
- 分支过滤保存成功但屏障未触发；
- 屏障抢占成功但排序消息未发送；
- 美化规划保存成功但美化消息未发送；
- 美化文件上传成功但数据库未提交；
- embedding 保存成功但匹配消息未发送；
- 匹配记录保存成功但图片未转 selected；
- 人工确认重复提交。

恢复任务必须根据数据库状态重新发布，不根据内存状态猜测。对象存储 key 必须确定性生成，使重复上传覆盖同一对象而不是制造重复文件。`processed_count`、`selected_count`、`rejected_count` 和 `not_selected_count` 只允许在终态转换成功时增加一次。

## 12. 测试矩阵

### 12.1 单元测试

必须新增或修改以下测试：

1. 五项完工事实全部满足才为 completed。
2. construction、insufficient_evidence、invalid_or_irrelevant 正确归一化。
3. completed 只读取完工过滤快照。
4. construction 只读取非完工过滤快照。
5. 证据不足和无效图直接拒绝，不调用分支过滤和美化。
6. 分支过滤响应协议不接受 beautify/content/tags 字段。
7. 任一过滤维度失败时整体必须 reject。
8. 过滤拒绝图片永远不能 claim 美化规划或美化。
9. 批次仍有一张 queued/analyzing 时不能发布任何美化规划。
10. 最后一张完成过滤时只发布一次 rank_job。
11. `max_selected=total_count` 也不能绕过批次屏障。
12. 两个分支通过的图片读取同一美化标准快照。
13. 不同图片可以得到不同美化参数。
14. 美化计划缺失、超限或不合法时不执行像素处理。
15. embedding 下载 key 来自美化后的 analysis_object_key。
16. 素材入库不调用 AI tagging provider。
17. 匹配不调用 feature_similarity。
18. `final_score == similarity_score` 且 `feature_score is null`。
19. matched 的标签与素材组标签快照完全相等。
20. pending_review 和 unmatched 的最终标签为空。
21. 人工确认后才写入素材组标签。
22. 历史任务仍能读取旧 ai_tags 和旧相似度字段。
23. 重复投递不重复美化、不重复匹配、不重复计数。

### 12.2 集成测试

准备至少包含以下图片的混合批次：

```text
完工有家具
完工无家具
完工但有少量包装和保护膜
毛坯
水电施工
泥木/油漆/安装阶段
施工节点局部图
完工证据不足的局部白墙
严重模糊或遮挡
效果图/图纸/商品图/室外图
重复图片
非 3:4 图片
```

数据库断言必须验证阶段顺序和标签来源，不能只看前端最终页面。

### 12.3 全链路验收

```powershell
alembic upgrade head
pytest -q
npm --prefix frontend run build
docker compose config
docker compose up -d --build
docker compose ps
```

再执行：

- API 健康检查；
- AI 配置连通性校验；
- 20 张混合批次冒烟；
- 100 张批次并发、恢复和重复投递测试；
- 浏览器桌面与移动尺寸结果页检查；
- Worker 日志检查，确认素材匹配阶段没有大模型请求；
- 数据库抽查，确认最终标签等于素材组标签快照。

任一命令或验收项失败时不得进入下一部署阶段。

## 13. 实施顺序

### P0：冻结基线和备份

1. 当前工作区已有大量未提交改动，先创建专用分支并提交一个可回退基线。
2. 备份数据库和对象存储关键数据。
3. 记录当前 `alembic current`、镜像版本、环境变量名和运行中 Worker 队列。
4. 运行现有测试并记录基线失败项；不允许把原有失败误归因于本次改造。

验收门：可从基线提交重新部署并读取历史任务。

### P1：锁定业务标准和响应协议

1. 导入本文件中的分类、完工过滤、非完工过滤和通用美化标准。
2. 为每套标准记录 ID、版本和快照。
3. 拆分过滤与美化规划 Pydantic Schema。
4. 删除新协议中的所有内容标签字段。

验收门：协议测试全部通过，错误字段会被明确拒绝。

### P2：数据库和状态机

1. 完成并验证现有双路由迁移。
2. 新增美化规划审计迁移。
3. 实现 claim/save/retry/recovery。
4. 修复真正的批次过滤屏障。

验收门：并发完成最后多张过滤任务时，只能有一个 Worker 启动后续阶段。

### P3：拆分美化

1. 分支过滤只返回过滤结果。
2. 屏障后启动美化规划队列。
3. `enhance` 只读取已完成、已校验的计划。
4. 接入小图预演、正式处理和质量验收。

验收门：数据库时间戳证明所有 `beautify_plan_started_at` 都晚于该 Job 最后的过滤完成时间。

### P4：素材库纯向量化和单一标签源

1. 素材入库移除大模型内容分析。
2. 目标图移除 analysis task。
3. 匹配改为 100% OpenCLIP 图片相似度。
4. 删除标签合并和回退。
5. 修复人工复核写入逻辑。

验收门：禁用 AI API Key 后，已有美化图的素材匹配仍能正常完成；最终标签只可能来自素材组。

### P5：API、前端和历史兼容

1. 增加 `library_tags` 响应。
2. 前端改用新字段和新文案。
3. 历史记录只读兼容。
4. 增加阶段进度和失败原因展示。

验收门：新任务页面无“AI 自动标签”概念；历史任务仍可打开。

### P6：标定、灰度和放量

1. 重建素材库同版本向量。
2. 运行标注集并标定阈值。
3. shadow 模式只记录结果，不自动继承标签。
4. 依次按内部任务、10%、50%、100% 放量。
5. 每一步至少观察一个完整业务周期。

验收门：达到约定精度、无跨分支、无越过屏障、无非素材库标签。

## 14. 功能开关与回滚

建议增加：

```text
COMPLETION_ROUTING_ENABLED
BATCH_FILTER_BARRIER_ENABLED
POST_FILTER_BEAUTIFY_PLAN_ENABLED
LIBRARY_IMAGE_ONLY_MATCHING_ENABLED
LIBRARY_ONLY_TAGS_ENABLED
LIBRARY_MATCH_SHADOW_MODE
```

回滚原则：

1. 数据库只做向前兼容迁移，不删除旧列，回滚应用时无需回滚表结构。
2. 新任务功能异常时停止创建新任务，不把已分类失败的图片自动改成非完工。
3. 屏障异常时宁可暂停美化，也不能恢复逐图越过屏障。
4. 匹配异常时图片可交付，但标签为空，不能恢复大模型标签兜底。
5. 模型提示词异常时回退提示词版本和标准快照。
6. OpenCLIP 版本异常时切回上一 embedding 版本及其完整素材索引，不能混用向量。
7. 回滚演练必须覆盖应用版本、Worker 队列、数据库兼容和历史结果读取。

## 15. 可观测性

至少记录以下指标：

```text
completion_requests_total
routed_filter_requests_total{branch}
filter_barrier_wait_seconds
filter_barrier_trigger_total
beautify_plan_requests_total
beautify_plan_failures_total
enhancement_failures_total
embedding_failures_total{source=job|library}
library_match_total{decision}
library_match_score
library_match_margin
library_tags_written_total{decision}
forbidden_llm_tag_write_total
```

日志必须包含 `job_id`、`image_id`、分类 subtype、实际过滤标准 ID/版本、美化标准 ID/版本、embedding 版本、匹配素材 ID、素材组 ID 和标签快照。禁止记录 API Key、完整 Base64 图片和未清理的供应商错误响应。

## 16. 最终验收标准

以下所有条件同时满足才算完工：

- 混合批次中的每张有效图片独立完成完工分类。
- 完工图只执行完工过滤标准，construction 只执行非完工过滤标准，错误分支次数为 0。
- 证据不足和无效图片不会被当成正常施工图放行。
- 任意图片开始美化规划前，该批次所有图片都已结束过滤阶段。
- 两个分支过滤通过的图片使用同一份美化标准快照。
- 过滤拒绝、失败或未入选图片不会生成美化图和匹配记录。
- OpenCLIP 查询向量来自美化后图片。
- 素材入库和素材匹配不调用大模型内容标签接口。
- 匹配分数只使用图片向量，不使用内容特征。
- 自动 matched 精度在人工标注集上达到至少 97%。
- matched 标签与素材组人工标签快照完全相等。
- pending_review、unmatched 和匹配失败的最终标签均为空。
- API 和前端不再把标签描述为 AI 自动生成。
- Worker 重试、超时恢复和重复投递不会重复美化、重复匹配或重复累计任务数量。
- 历史任务和旧结果仍可读取。
- 迁移、后端测试、前端构建、Compose 配置、20 张冒烟和 100 张并发测试全部通过。
- 功能开关和回滚流程完成一次实际演练。

## 17. 执行检查表

```text
[ ] 建立可回退基线并完成数据备份
[ ] 固化四套业务标准及版本
[ ] 完成双路由迁移验证
[ ] 新增美化规划审计迁移
[ ] 分离过滤和美化规划响应协议
[ ] 修复真正的整批过滤屏障
[ ] 新增 beautify_plan Worker 和恢复逻辑
[ ] enhance 只接受已完成计划
[ ] 素材入库移除大模型分析
[ ] 目标图移除 AnalysisTaskPublisher
[ ] 相似度改为 100% 图片向量
[ ] matched 只复制素材组标签
[ ] pending_review/unmatched 标签为空
[ ] API 增加 library_tags
[ ] 前端移除 AI 自动标签文案
[ ] 补齐单元、集成、并发和恢复测试
[ ] 重建素材向量并标定阈值
[ ] shadow 验证
[ ] 10%/50%/100% 灰度
[ ] 完成回滚演练
[ ] 对照第 16 节逐项签字验收
```

执行时不得跳过任何“验收门”。这份方案通过阶段隔离、数据库状态约束、单一标签来源、自动化测试、shadow 灰度和可回滚迁移降低出错概率；在真实图片识别场景中不能承诺模型永远零误判，因此必须以人工标注集指标和灰度结果作为最终上线依据。
