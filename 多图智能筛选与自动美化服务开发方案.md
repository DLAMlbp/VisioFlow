# 多图智能筛选与自动美化服务开发方案

> 项目代号：Image Intelligence Service  
> 项目定位：为 App / Web / 小程序提供「多图智能筛选 + 排序 + 自动美化 + 高速批处理」能力  
> 使用方式：本文件可直接作为 Codex / Claude Code / AI Coding Agent 的项目开发规格  
> 核心原则：**速度优先、规则可配置、模块可替换、结果可解释、成本可控、支持横向扩容**

---

# 1. 项目目标

需要开发一个独立图片智能处理服务，供 App 通过 API 调用。

用户一次上传多张图片后，系统自动完成：

1. 图片合法性检查；
2. 图片基础质量检测；
3. 按业务标准过滤不合格图片；
4. 对图片进行综合评分；
5. 对重复 / 近似图片自动去重；
6. 对保留图片进行排序；
7. 对合格图片进行自然美化；
8. 将评分、淘汰原因、排序、优化后图片返回 App；
9. 整个任务支持异步并发；
10. 尽可能降低单次多图处理时间。

最终产品不是单纯的“图片美化 API”，而是：

> **智能图片决策与增强服务**

核心拆成三个引擎：

```text
Image Filter Engine
图片过滤引擎
    ↓
判断哪些图片不能用

Image Rank Engine
图片排序引擎
    ↓
判断哪些图片最好

Image Enhance Engine
图片增强引擎
    ↓
自动把合格图片优化得更好
```

---

# 2. 第一性原理

系统处理速度取决于：

```text
总耗时
=
上传耗时
+
图片解码耗时
+
检测耗时
+
模型推理耗时
+
美化耗时
+
存储耗时
```

因此不能简单地：

```text
所有图片
→ 全部分辨率运行 AI
→ 所有图片进行美化
→ 再筛选
```

必须采用：

```text
低成本任务先做
↓
不合格图片提前退出
↓
缩略图完成绝大多数判断
↓
只对合格图片读取原图
↓
只对合格图片进行美化
```

即：

> **Early Exit + Thumbnail First + Parallel Processing + Batch Inference**

---

# 3. 产品处理流程

完整流程：

```text
App
 │
 │ 上传 N 张图片
 ▼
对象存储
 │
 ▼
创建 Image Job
 │
 ▼
生成缩略图
 │
 ▼
硬规则快速过滤
 │
 ├── 不合格 ─────────→ Reject
 │
 ▼
基础质量检测
 │
 ▼
人脸 / 主体分析
 │
 ▼
重复 / 近似图分析
 │
 ▼
业务规则计算
 │
 ▼
综合评分
 │
 ▼
多图排序
 │
 ▼
筛选最佳图片
 │
 ▼
自动美化
 │
 ▼
保存输出图片
 │
 ▼
返回 App
```

---

# 4. MVP 范围

第一版必须形成完整闭环。

## 4.1 图片基础检测

实现：

- 文件是否损坏；
- MIME Type；
- 图片宽高；
- 图片比例；
- 文件大小；
- EXIF Orientation；
- 清晰度；
- 曝光；
- 过曝；
- 欠曝；
- 对比度；
- 纯色图片；
- 极端异常图片。

---

## 4.2 图片智能筛选

实现：

- 清晰度评分；
- 曝光评分；
- 对比度评分；
- 分辨率评分；
- 构图评分；
- 主体评分；
- 人脸完整度评分；
- 重复度判断；
- 综合评分；
- 自动排序；
- 自动淘汰；
- 自动保留。

---

## 4.3 多图去重

支持：

### 完全重复

```text
SHA256
```

### 视觉近似

```text
pHash
+
Embedding
```

先快速粗筛：

```text
pHash
```

再精确判断：

```text
CLIP / SigLIP Embedding
```

---

## 4.4 图片美化

第一版只做自然增强：

- 自动曝光；
- 自动对比度；
- 白平衡；
- Gamma；
- 阴影恢复；
- 高光压制；
- 饱和度微调；
- 锐化；
- 轻量降噪；
- 输出压缩；
- EXIF 修正。

原则：

> **图片变得更好，但不能明显看出被 AI 修改。**

---

# 5. 暂不实现

第一版不要开发：

- AI 换脸；
- AI 五官重塑；
- 大幅磨皮；
- AI 换背景；
- AI 服装替换；
- AI 生图重绘；
- Generative Fill；
- Outpainting；
- 视频处理；
- RAW 专业修图；
- Photoshop 级局部处理。

这些属于：

```text
Level 3 Generative Editing
```

以后单独开发。

---

# 6. 总体技术架构

推荐：

```text
┌────────────────────────────┐
│            APP             │
└─────────────┬──────────────┘
              │ HTTPS
              ▼
┌────────────────────────────┐
│         API Gateway        │
│          FastAPI           │
└──────┬──────────┬──────────┘
       │          │
       │          ├───────────────┐
       ▼          ▼               ▼
 PostgreSQL     Redis        Object Storage
 Job / Rule     Queue        S3 / MinIO / OSS
       │          │
       └────┬─────┘
            ▼
┌────────────────────────────┐
│      Task Dispatcher       │
└─────────────┬──────────────┘
              │
      ┌───────┼─────────┐
      ▼       ▼         ▼
Preprocess  Analysis  Enhance
 Worker      Worker    Worker
      │       │         │
      └───────┼─────────┘
              ▼
┌────────────────────────────┐
│      Scoring Engine        │
│      Rule + AI Score       │
└─────────────┬──────────────┘
              ▼
        Object Storage
              │
              ▼
          Result API
              │
              ▼
             APP
```

---

# 7. 推荐技术栈

## 7.1 后端

```text
Python 3.12+
FastAPI
Pydantic v2
SQLAlchemy 2
Alembic
PostgreSQL 16+
Redis 7+
Celery
```

第一版推荐：

> FastAPI + PostgreSQL + Redis + Celery

原因：

- Python AI / CV 生态最好；
- FastAPI 易于开发 API；
- OpenAPI 自动生成；
- Celery 适合异步任务；
- Redis 可同时承担 Queue / Cache；
- PostgreSQL 保存任务和规则配置。

---

# 8. 图像处理技术

基础：

```text
OpenCV
Pillow
NumPy
scikit-image
```

AI 推理：

```text
PyTorch
ONNX Runtime
TensorRT
```

优先级：

```text
OpenCV
>
ONNX
>
PyTorch
>
大型 Vision / Generative Model
```

即：

> 能用低成本算法解决的问题不要调用大模型。

---

# 9. 对象存储

开发环境：

```text
MinIO
```

生产环境支持：

```text
Alibaba Cloud OSS
Tencent COS
AWS S3
Cloudflare R2
MinIO
```

必须实现统一 Storage Adapter：

```python
class StorageProvider:
    async def upload(...)
    async def download(...)
    async def delete(...)
    async def presign_upload(...)
    async def presign_download(...)
```

业务层禁止直接依赖具体云厂商 SDK。

---

# 10. Repository 目录结构

Codex 按下面结构建立项目：

```text
image-intelligence-service/
│
├── README.md
├── pyproject.toml
├── Dockerfile
├── docker-compose.yml
├── .env.example
├── Makefile
│
├── src/
│   ├── main.py
│   │
│   ├── api/
│   │   ├── router.py
│   │   ├── health.py
│   │   ├── jobs.py
│   │   ├── uploads.py
│   │   ├── filter_profiles.py
│   │   └── beautify_profiles.py
│   │
│   ├── core/
│   │   ├── config.py
│   │   ├── logging.py
│   │   ├── security.py
│   │   ├── exceptions.py
│   │   └── constants.py
│   │
│   ├── db/
│   │   ├── base.py
│   │   ├── session.py
│   │   └── migrations/
│   │
│   ├── models/
│   │   ├── image_job.py
│   │   ├── image_item.py
│   │   ├── image_metric.py
│   │   ├── image_result.py
│   │   └── profile.py
│   │
│   ├── schemas/
│   │
│   ├── repositories/
│   │
│   ├── services/
│   │   ├── jobs/
│   │   ├── storage/
│   │   ├── profiles/
│   │   └── callbacks/
│   │
│   ├── pipeline/
│   │   ├── base.py
│   │   ├── context.py
│   │   ├── metadata.py
│   │   ├── hard_filter.py
│   │   ├── quality.py
│   │   ├── face.py
│   │   ├── subject.py
│   │   ├── duplicate.py
│   │   ├── scoring.py
│   │   └── enhance.py
│   │
│   ├── inference/
│   │   ├── interfaces.py
│   │   ├── quality/
│   │   ├── face/
│   │   ├── subject/
│   │   └── embedding/
│   │
│   ├── rules/
│   │   ├── base.py
│   │   ├── image_size.py
│   │   ├── blur.py
│   │   ├── exposure.py
│   │   └── duplicate.py
│   │
│   ├── profiles/
│   │   ├── filters/
│   │   └── beautify/
│   │
│   ├── workers/
│   │   ├── celery_app.py
│   │   ├── preprocess.py
│   │   ├── analysis.py
│   │   ├── ranking.py
│   │   ├── enhance.py
│   │   └── callback.py
│   │
│   └── monitoring/
│
└── tests/
    ├── unit/
    ├── integration/
    └── fixtures/
```

---

# 11. Processing Pipeline

所有图片处理必须使用统一 Pipeline。

禁止把全部处理逻辑写进一个函数。

基础接口：

```python
from abc import ABC, abstractmethod

class PipelineStage(ABC):
    name: str

    @abstractmethod
    async def run(self, context):
        pass
```

执行：

```text
MetadataStage
      ↓
HardFilterStage
      ↓
QualityStage
      ↓
FaceStage
      ↓
SubjectStage
      ↓
EmbeddingStage
      ↓
ScoreStage
```

单图分析结束后：

```text
GroupDuplicateStage
        ↓
GroupRankingStage
        ↓
SelectionStage
        ↓
EnhanceStage
```

---

# 12. ProcessingContext

统一使用 Context 在 Pipeline 中传递数据。

```python
@dataclass
class ProcessingContext:
    job_id: str
    image_id: str

    metadata: dict
    quality: dict

    faces: list
    subject: dict

    embedding: list | None

    scores: dict
    warnings: list
    reject_codes: list

    decision: str | None
    final_score: float | None

    original_object_key: str
    thumbnail_object_key: str | None
    enhanced_object_key: str | None
```

禁止在多个 Stage 之间传递大量独立参数。

---

# 13. Stage 0：图片上传

生产环境推荐：

```text
App
 ↓
请求 Presigned URL
 ↓
App 直接上传 Storage
 ↓
获取 object_key
 ↓
创建 Job
```

不要让大图片全部经过 FastAPI Server。

接口：

```http
POST /api/v1/uploads/presign
```

Request：

```json
{
  "filename": "IMG_001.jpg",
  "content_type": "image/jpeg",
  "file_size": 4567281
}
```

Response：

```json
{
  "object_key": "uploads/2026/08/xxx.jpg",
  "upload_url": "https://..."
}
```

---

# 14. Stage 1：Metadata

读取：

```text
width
height
format
mime
file_size
aspect_ratio
EXIF
orientation
```

生成：

```text
512px / 768px Thumbnail
```

绝大多数 AI 分析优先使用 Thumbnail。

---

# 15. Stage 2：Hard Filter

必须高速完成。

规则包括：

```text
INVALID_FILE
IMAGE_CORRUPTED
UNSUPPORTED_FORMAT
IMAGE_TOO_SMALL
IMAGE_TOO_LARGE
EXTREME_BLUR
EXTREME_OVEREXPOSURE
EXTREME_UNDEREXPOSURE
SOLID_COLOR
```

原则：

```text
如果图片已经必然被淘汰
→ 不再调用后续 AI 模型
```

这称为：

> Early Exit

---

# 16. 清晰度检测

第一版：

```text
Laplacian Variance
+
Tenengrad
```

原始数值需要归一化为：

```text
0 - 100
```

输出：

```json
{
  "sharpness_score": 84.3,
  "blur_level": "low"
}
```

后续保留替换深度学习 Quality Model 的能力。

---

# 17. 曝光检测

检测：

```text
Mean Brightness
Histogram
Highlight Clipping
Shadow Clipping
Dynamic Range
```

输出：

```json
{
  "exposure_score": 88,
  "overexposed_ratio": 0.02,
  "underexposed_ratio": 0.06,
  "brightness_mean": 128.4
}
```

---

# 18. 对比度检测

计算：

```text
灰度标准差
局部对比度
动态范围
```

输出：

```text
contrast_score
0 - 100
```

---

# 19. 人脸分析

第一版需要支持：

- 人脸检测；
- 人脸数量；
- 最大人脸占比；
- 人脸是否被裁切；
- 人脸清晰度。

输出：

```json
{
  "face_count": 3,
  "largest_face_ratio": 0.16,
  "faces": [
    {
      "bbox": [0.2, 0.1, 0.45, 0.52],
      "confidence": 0.98,
      "cropped": false
    }
  ]
}
```

第二阶段增加：

- 闭眼；
- 头部姿态；
- 表情；
- 遮挡；
- 人脸质量。

---

# 20. 主体检测

输出：

```json
{
  "subject_count": 1,
  "primary_subject_ratio": 0.36,
  "center_x": 0.48,
  "center_y": 0.41,
  "cropped": false
}
```

用于判断：

```text
主体是否过小
主体是否完整
主体是否偏离画面
主体是否明显
```

---

# 21. 构图评分

第一版采用规则组合，不急于使用大型 AI。

维度：

```text
主体位置
人脸位置
主体大小
三分法
边缘裁切
画面平衡度
地平线
视觉中心
```

输出：

```text
composition_score
0 - 100
```

---

# 22. 重复图片检测

## 22.1 Exact Duplicate

```text
SHA256
```

完全相同：

```text
duplicate_exact = true
```

---

## 22.2 Near Duplicate

第一阶段：

```text
pHash
```

当 Hamming Distance 小于阈值：

```text
进入候选重复组
```

第二阶段：

```text
CLIP / SigLIP Embedding
Cosine Similarity
```

例如：

```text
similarity >= 0.96
→ Near Duplicate
```

阈值必须配置化。

---

# 23. Duplicate Group

输出：

```json
{
  "group_id": "dup_001",
  "images": [
    "img_001",
    "img_004",
    "img_011"
  ]
}
```

组内：

```text
保留 Final Score 最高的一张
```

其他标记：

```text
DUPLICATE_LOWER_SCORE
```

---

# 24. Filter Profile

所有筛选标准必须配置化。

禁止写死在代码中。

例如：

```text
general_v1
xiaohongshu_v1
portrait_v1
business_event_v1
product_v1
ppt_v1
```

调用时：

```json
{
  "filter_profile": "business_event_v1"
}
```

---

# 25. Filter Profile Schema

建议 YAML：

```yaml
id: business_event_v1
version: 1

hard_rules:
  min_width: 1080
  min_height: 720
  max_file_size_mb: 25

  reject_invalid_image: true
  reject_extreme_blur: true

  max_overexposure_ratio: 0.35
  max_underexposure_ratio: 0.45

weights:
  sharpness: 0.20
  exposure: 0.10
  contrast: 0.05
  composition: 0.15
  face_quality: 0.20
  subject_quality: 0.15
  content_quality: 0.10
  uniqueness: 0.05

thresholds:
  reject: 50
  candidate: 65
  good: 75
  selected: 82

duplicate:
  enabled: true
  phash_distance: 8
  embedding_similarity: 0.96
  keep_best: true
```

---

# 26. Rule Engine

统一接口：

```python
class Rule:
    code: str

    async def evaluate(
        self,
        context: ProcessingContext,
        config: dict
    ) -> "RuleResult":
        ...
```

结果：

```python
@dataclass
class RuleResult:
    passed: bool
    code: str | None
    reason: str | None
    score_delta: float = 0
```

---

# 27. 综合评分

第一版：

```text
Final Score
=
Sharpness × W1
+
Exposure × W2
+
Contrast × W3
+
Composition × W4
+
Face × W5
+
Subject × W6
+
Content × W7
+
Uniqueness × W8
```

统一：

```text
0 - 100
```

权重来自 Filter Profile。

---

# 28. 评分决策

默认：

```text
Score < 50
→ Rejected

50 <= Score < 65
→ Candidate

65 <= Score < 75
→ Good

Score >= 75
→ Selected
```

实际阈值完全由 Profile 控制。

---

# 29. 可解释输出

每一张图片必须返回：

```text
Final Score
Rank
Decision
Metrics
Reasons
Warnings
Reject Codes
```

例如：

```json
{
  "image_id": "img_001",
  "score": 88.6,
  "rank": 2,
  "decision": "selected",
  "reasons": [
    "主体清晰",
    "曝光正常",
    "人物完整"
  ],
  "warnings": [
    "主体轻微偏左"
  ]
}
```

淘汰：

```json
{
  "image_id": "img_007",
  "score": 43.5,
  "decision": "rejected",
  "reject_codes": [
    "EXTREME_BLUR"
  ]
}
```

---

# 30. Beautify Engine

必须独立于 Filter Engine。

```text
Filter
    ↓
Selected
    ↓
Beautify
```

禁止：

```text
所有上传图片
→ 全部美化
→ 然后筛选
```

---

# 31. 美化级别

## Level 0

```text
不处理
```

---

## Level 1：极速增强

默认。

实现：

```text
Auto Exposure
Auto Contrast
White Balance
Gamma
Highlights
Shadows
Saturation
Sharpen
Resize
Compression
```

推荐 CPU / OpenCV 实现。

---

## Level 2：智能增强

可选 GPU。

增加：

```text
AI Denoise
Light Face Restore
Skin Tone Correction
Local Exposure
Super Resolution
```

要求：

```text
不改变人物身份
不改变五官
不改变主体
不重绘图片
```

---

## Level 3：生成式精修

未来独立模块：

```text
Generative Fill
Background Replace
Outpaint
Object Remove
AI Restore
```

第一版不开发。

---

# 32. Beautify Profile

示例：

```yaml
id: natural_v1
version: 1

exposure:
  enabled: true
  strength: 0.25

contrast:
  enabled: true
  strength: 0.15

white_balance:
  enabled: true
  strength: 0.30

highlights:
  enabled: true
  strength: 0.15

shadows:
  enabled: true
  strength: 0.15

saturation:
  enabled: true
  strength: 0.05

sharpen:
  enabled: true
  strength: 0.10

denoise:
  enabled: false

face_restore:
  enabled: false

face_reshape:
  enabled: false
```

---

# 33. 极速处理策略

必须实现以下性能策略。

---

## 33.1 Thumbnail First

```text
原图 4K
 │
 ├──生成 512 / 768 Thumbnail
 │        ↓
 │      检测
 │        ↓
 │      评分
 │
 └────────如果 Selected
             ↓
           加载原图
             ↓
           Beautify
```

---

## 33.2 Early Exit

以下情况直接结束：

```text
文件损坏
尺寸严重不足
严重模糊
严重曝光
不支持格式
```

不要继续调用：

```text
Face
Subject
Embedding
Vision Model
Beautify
```

---

## 33.3 Parallel Processing

同一个 Job 中：

```text
image1 ─┐
image2 ─┤
image3 ─┤
image4 ─┤→ Worker Pool
image5 ─┤
...     ┘
```

禁止串行处理 20 张图片。

---

## 33.4 Batch Inference

GPU 模型必须支持：

```python
model.predict([
    image1,
    image2,
    image3,
    image4
])
```

推荐 batch：

```text
8
16
32
```

根据显存动态控制。

---

# 34. Worker 划分

建议：

```text
preprocess_worker
analysis_worker
ranking_worker
enhance_worker
callback_worker
```

原因：

不同阶段：

```text
CPU / GPU / IO
```

负载不同。

---

# 35. Job 状态

```text
CREATED
UPLOADING
QUEUED
ANALYZING
RANKING
ENHANCING
COMPLETED
PARTIAL_FAILED
FAILED
CANCELLED
```

---

# 36. API

统一前缀：

```text
/api/v1
```

---

# 37. 创建图片 Job

```http
POST /api/v1/image/jobs
```

Request：

```json
{
  "filter_profile": "business_event_v1",
  "beautify_profile": "natural_v1",
  "enhance_level": 1,
  "max_selected": 10,
  "images": [
    {
      "object_key": "uploads/a.jpg"
    },
    {
      "object_key": "uploads/b.jpg"
    }
  ],
  "callback_url": "https://app.example.com/callback"
}
```

Response：

```json
{
  "job_id": "job_01JXXXX",
  "status": "queued",
  "total": 2
}
```

---

# 38. 查询 Job

```http
GET /api/v1/image/jobs/{job_id}
```

Response：

```json
{
  "job_id": "job_01JXXXX",
  "status": "analyzing",
  "progress": 65,
  "total": 20,
  "processed": 13,
  "selected": 5,
  "rejected": 8
}
```

---

# 39. 获取结果

```http
GET /api/v1/image/jobs/{job_id}/results
```

Response：

```json
{
  "job_id": "job_01JXXXX",
  "summary": {
    "total": 20,
    "selected": 8,
    "rejected": 12
  },
  "images": [
    {
      "image_id": "img_001",
      "rank": 1,
      "score": 92.5,
      "decision": "selected",
      "original_url": "https://...",
      "enhanced_url": "https://...",
      "metrics": {
        "sharpness": 95,
        "exposure": 88,
        "contrast": 82,
        "composition": 91,
        "face_quality": 92,
        "subject_quality": 90
      },
      "reasons": [
        "主体清晰",
        "人物完整",
        "构图优秀"
      ],
      "warnings": []
    }
  ]
}
```

---

# 40. Callback

Job 完成后：

```http
POST callback_url
```

Body：

```json
{
  "event": "image.job.completed",
  "job_id": "job_01JXXXX",
  "status": "completed"
}
```

必须：

```text
Timeout
Retry
Signature
```

---

# 41. Profile API

```http
GET    /api/v1/filter-profiles
GET    /api/v1/filter-profiles/{id}
POST   /api/v1/filter-profiles
PUT    /api/v1/filter-profiles/{id}

GET    /api/v1/beautify-profiles
GET    /api/v1/beautify-profiles/{id}
POST   /api/v1/beautify-profiles
PUT    /api/v1/beautify-profiles/{id}
```

---

# 42. 数据库

核心表：

```text
image_jobs
image_items
image_metrics
image_results
filter_profiles
beautify_profiles
duplicate_groups
```

---

# 43. image_jobs

字段：

```text
id
user_id
status

filter_profile_id
beautify_profile_id
enhance_level

max_selected

total_count
processed_count
selected_count
rejected_count

callback_url

created_at
started_at
completed_at
updated_at
```

---

# 44. image_items

```text
id
job_id

object_key
thumbnail_object_key

original_filename
content_type
file_size

width
height
aspect_ratio

sha256
phash

status

created_at
updated_at
```

---

# 45. image_metrics

```text
id
image_id

sharpness_score
exposure_score
contrast_score
noise_score

composition_score
face_score
subject_score
content_score
uniqueness_score

final_score

raw_metrics_json
model_versions_json
```

---

# 46. image_results

```text
id
image_id

decision
rank

enhanced_object_key

reject_codes_json
reasons_json
warnings_json

created_at
```

---

# 47. Redis

用途：

```text
Celery Queue
Job Progress
Profile Cache
Image Analysis Cache
Distributed Lock
Rate Limit
```

Key：

```text
job:{job_id}:progress

profile:filter:{profile_id}:{version}

profile:beautify:{profile_id}:{version}

image:{sha256}:analysis:{model_version}

image:{sha256}:enhance:{profile}:{version}
```

---

# 48. 缓存策略

如果同一图片被重复上传：

```text
SHA256
 ↓
找到已有 Analysis
 ↓
直接复用
```

缓存：

```text
metadata
quality
face
subject
embedding
```

美化结果缓存 Key：

```text
SHA256
+
Beautify Profile ID
+
Beautify Profile Version
+
Enhance Engine Version
```

---

# 49. API 鉴权

支持：

```text
API Key
```

未来：

```text
JWT
OAuth
```

Header：

```http
Authorization: Bearer xxx
```

或：

```http
X-API-Key: xxx
```

---

# 50. 文件安全

必须验证：

```text
MIME
Magic Bytes
Image Decoder
File Size
Pixel Size
```

禁止只根据：

```text
.jpg
.png
```

判断图片是否合法。

---

# 51. 用户隐私

图片默认存储：

```text
Private Bucket
```

访问：

```text
Presigned URL
```

配置：

```env
IMAGE_RETENTION_DAYS=7
```

支持自动清理。

日志禁止记录：

```text
原始图片内容
Base64
完整签名 URL
```

---

# 52. 性能目标

MVP 目标：

## 单图基础分析

```text
P50 < 300 ms
P95 < 800 ms
```

不含上传。

---

## Level 1 Beautify

目标：

```text
P50 < 300 ms / selected image
```

---

## 10 张图片

目标：

```text
2 - 5 秒
```

---

## 20 张图片

目标：

```text
3 - 8 秒
```

以上是工程目标，不是绝对 SLA。

必须通过 Benchmark 实测。

---

# 53. 并发目标

MVP：

```text
单 Job 最大 50 张
```

环境变量：

```env
MAX_IMAGES_PER_JOB=50
```

第一版压力测试：

```text
10 concurrent jobs
×
20 images
=
200 images
```

---

# 54. 可观测性

需要监控：

```text
api_request_latency

job_total_latency

preprocess_latency
quality_latency
face_latency
embedding_latency
enhance_latency

queue_depth

worker_busy

cpu_usage

gpu_usage
gpu_memory

job_success_rate
job_failure_rate
image_reject_rate
```

推荐：

```text
Prometheus
Grafana
Sentry
OpenTelemetry
```

---

# 55. Structured Logging

禁止：

```python
print(...)
```

统一 JSON Log：

```json
{
  "timestamp": "...",
  "job_id": "...",
  "image_id": "...",
  "stage": "quality",
  "duration_ms": 82,
  "status": "success"
}
```

---

# 56. GPU 架构

API Server 和 GPU Worker 解耦。

```text
API
 ↓
Redis
 ↓
GPU Worker 1
GPU Worker 2
GPU Worker 3
```

通过增加 GPU Worker 横向扩容。

---

# 57. 推理模型抽象

业务代码禁止直接绑定模型。

定义：

```python
class QualityModel:
    async def predict(self, images):
        ...

class FaceModel:
    async def predict(self, images):
        ...

class SubjectModel:
    async def predict(self, images):
        ...

class EmbeddingModel:
    async def encode(self, images):
        ...
```

这样未来可以替换：

```text
PyTorch
ONNX
TensorRT
Remote API
```

---

# 58. 模型版本

结果必须记录：

```text
quality_model_version
face_model_version
embedding_model_version
enhance_engine_version
filter_profile_version
beautify_profile_version
```

保证未来可以定位：

> 为什么同一张图片两个月后的评分不一样。

---

# 59. 默认 General Filter Profile

```yaml
id: general_v1
version: 1

hard_rules:
  min_width: 720
  min_height: 720
  max_file_size_mb: 25

  reject_invalid_image: true
  reject_extreme_blur: true

weights:
  sharpness: 0.25
  exposure: 0.15
  contrast: 0.10
  composition: 0.15
  face_quality: 0.10
  subject_quality: 0.15
  content_quality: 0.00
  uniqueness: 0.10

thresholds:
  reject: 50
  candidate: 65
  good: 75
  selected: 82

duplicate:
  enabled: true
  phash_distance: 8
  embedding_similarity: 0.96
  keep_best: true
```

---

# 60. 默认 Natural Beautify Profile

```yaml
id: natural_v1
version: 1

exposure:
  enabled: true
  strength: 0.25

contrast:
  enabled: true
  strength: 0.15

white_balance:
  enabled: true
  strength: 0.30

highlights:
  enabled: true
  strength: 0.15

shadows:
  enabled: true
  strength: 0.15

saturation:
  enabled: true
  strength: 0.05

sharpen:
  enabled: true
  strength: 0.10

denoise:
  enabled: false

face_restore:
  enabled: false

face_reshape:
  enabled: false
```

---

# 61. Docker

必须提供：

```text
Dockerfile
docker-compose.yml
```

Compose：

```text
api
worker-preprocess
worker-analysis
worker-ranking
worker-enhance
worker-callback

postgres
redis
minio
```

---

# 62. 环境变量

创建：

```text
.env.example
```

内容：

```env
APP_ENV=development
APP_HOST=0.0.0.0
APP_PORT=8000

DATABASE_URL=postgresql+asyncpg://user:password@postgres:5432/image_ai

REDIS_URL=redis://redis:6379/0

S3_ENDPOINT=http://minio:9000
S3_ACCESS_KEY=minio
S3_SECRET_KEY=minio123
S3_BUCKET=image-ai
S3_REGION=us-east-1

INFERENCE_DEVICE=cpu

MAX_IMAGES_PER_JOB=50
MAX_IMAGE_SIZE_MB=25

THUMBNAIL_LONG_SIDE=768

DEFAULT_FILTER_PROFILE=general_v1
DEFAULT_BEAUTIFY_PROFILE=natural_v1

IMAGE_RETENTION_DAYS=7
```

---

# 63. 错误码

系统错误：

```text
JOB_NOT_FOUND
PROFILE_NOT_FOUND

INVALID_IMAGE
UNSUPPORTED_FORMAT
IMAGE_CORRUPTED

STORAGE_FAILED
DATABASE_FAILED
QUEUE_FAILED

INFERENCE_FAILED
PROCESSING_TIMEOUT

CALLBACK_FAILED
```

图片淘汰原因：

```text
IMAGE_TOO_SMALL
EXTREME_BLUR
EXTREME_OVEREXPOSURE
EXTREME_UNDEREXPOSURE
SOLID_COLOR

FACE_CROPPED
SUBJECT_TOO_SMALL

DUPLICATE_LOWER_SCORE

LOW_TOTAL_SCORE
```

系统错误和业务淘汰原因禁止混用。

---

# 64. Retry

Celery Task 必须支持：

```text
retry
exponential backoff
max retries
timeout
```

例如：

```text
Storage Network Error
→ Retry

Model Runtime Error
→ Retry

Invalid Image
→ 不 Retry
```

---

# 65. Idempotency

创建任务建议支持：

```http
Idempotency-Key
```

防止 App 网络重试导致重复创建 Job。

---

# 66. App 交互流程

推荐 App：

```text
用户选择 20 张图片
        ↓
获取 Upload URL
        ↓
并发上传
        ↓
创建 Job
        ↓
立即获得 job_id
        ↓
显示「智能筛选中」
        ↓
查询进度
        ↓
展示最佳图片
```

---

# 67. App 轮询

如果没有 WebSocket：

```text
1s
2s
3s
5s
5s
...
```

指数退避。

Job 完成后停止。

---

# 68. WebSocket

第二阶段可以：

```text
/ws/jobs/{job_id}
```

实时返回：

```json
{
  "event": "progress",
  "progress": 68
}
```

MVP 不强制。

---

# 69. Phase 1：项目骨架

Codex 执行：

```text
1. 初始化 Python 3.12 项目。
2. 创建 FastAPI。
3. 配置 Pydantic Settings。
4. 配置 PostgreSQL。
5. 配置 Redis。
6. 配置 Celery。
7. 配置 MinIO。
8. 创建 Docker Compose。
9. 创建 Alembic。
10. 创建 /health。
```

验收：

```http
GET /health
```

返回：

```json
{
  "status": "ok"
}
```

---

# 70. Phase 2：Storage + Upload

实现：

```text
Storage Adapter
MinIO Provider
Presigned Upload
Presigned Download
```

测试：

```text
上传 JPEG
上传 PNG
读取图片
删除图片
```

---

# 71. Phase 3：Job System

实现：

```text
Create Job
Get Job
Job Progress
Job State Machine
```

实现数据表：

```text
image_jobs
image_items
```

---

# 72. Phase 4：Image Metadata

实现：

```text
Image Decode
MIME
Magic Bytes
Width
Height
File Size
EXIF Orientation
Thumbnail
SHA256
```

---

# 73. Phase 5：Hard Filter

实现：

```text
Invalid
Image Size
Extreme Blur
Extreme Exposure
Solid Color
```

并完成：

```text
Early Exit
```

---

# 74. Phase 6：Quality Engine

实现：

```text
Sharpness
Exposure
Contrast
Noise Estimate
```

统一返回：

```text
0 - 100
```

---

# 75. Phase 7：Filter Profile

实现：

```text
YAML Profile Loader
DB Profile
Profile Version
Profile Cache
Rule Engine
```

重点：

> 所有阈值不得硬编码。

---

# 76. Phase 8：Duplicate Engine

实现：

```text
SHA256
pHash
Duplicate Group
```

然后再加入：

```text
Embedding
Cosine Similarity
```

---

# 77. Phase 9：Scoring + Ranking

实现：

```text
Weighted Score
Decision
Rank
max_selected
Duplicate Keep Best
```

---

# 78. Phase 10：Beautify Engine

实现 Level 1：

```text
Exposure
Contrast
White Balance
Highlights
Shadows
Saturation
Sharpen
Output Compression
```

---

# 79. Phase 11：并行任务

优化：

```text
Worker Pool
Parallel Per Image
Group Barrier
Ranking
Enhance
```

必须保证：

```text
所有图片分析完成
→ 才进行 Group Ranking
```

---

# 80. Phase 12：人脸 / 主体

增加：

```text
Face Detection
Face Cropping Detection
Subject Detection
Subject Ratio
```

---

# 81. Phase 13：性能优化

实现：

```text
Thumbnail First
Batch Inference
Cache
Connection Pool
Storage Parallelism
DB Bulk Insert
```

---

# 82. Phase 14：Monitoring

加入：

```text
Prometheus
Sentry
Structured Logging
```

---

# 83. Phase 15：Production Hardening

加入：

```text
Authentication
Rate Limit
Cleanup
Retry
Timeout
Idempotency
Security
```

---

# 84. 单元测试

至少覆盖：

```text
Metadata
Blur
Exposure
Filter Rule
Score
Ranking
Duplicate
Beautify
Profile
```

---

# 85. Integration Test

场景 1：

```text
上传 5 张正常图片
→ Job Completed
```

场景 2：

```text
1 张损坏
+
4 张正常
→ Partial / Completed
→ 损坏图片单独 Reject
```

场景 3：

```text
3 张重复
→ 只保留最高分
```

场景 4：

```text
20 张
→ 全流程完成
```

---

# 86. Benchmark

建立：

```text
tests/benchmark/
```

记录：

```text
1 image
5 images
10 images
20 images
50 images
```

指标：

```text
total time

preprocess
analysis
duplicate
ranking
enhance
storage
```

---

# 87. Definition of Done

MVP 只有全部满足以下条件才算完成。

## 功能

- [ ] 一次提交至少 20 张图片；
- [ ] App 直传对象存储；
- [ ] 异步 Job；
- [ ] Job Progress；
- [ ] 图片合法性检测；
- [ ] 分辨率检测；
- [ ] 清晰度检测；
- [ ] 曝光检测；
- [ ] 对比度检测；
- [ ] 重复图片检测；
- [ ] 综合评分；
- [ ] 自动排序；
- [ ] 自动淘汰；
- [ ] 自动保留；
- [ ] 基础美化；
- [ ] 返回原图 URL；
- [ ] 返回优化图 URL；
- [ ] 返回淘汰原因；
- [ ] Filter Profile；
- [ ] Beautify Profile。

## 工程

- [ ] Docker Compose 一键启动；
- [ ] PostgreSQL；
- [ ] Redis；
- [ ] Celery；
- [ ] MinIO；
- [ ] Alembic；
- [ ] OpenAPI；
- [ ] Unit Test；
- [ ] Integration Test；
- [ ] Structured Logging；
- [ ] Error Handling；
- [ ] Retry；
- [ ] Timeout；
- [ ] Cache；
- [ ] Benchmark。

---

# 88. Codex 总开发约束

将下面内容作为 Codex 的最高开发约束：

```text
你正在开发一个生产级多图智能筛选与自动美化服务。

必须遵守：

1. 不要一次性实现整个项目，严格按 Phase 开发。
2. 每完成一个 Phase，必须运行测试。
3. 每完成一个 Phase，必须确保现有 API 不被破坏。
4. 所有数据库结构修改必须通过 Alembic Migration。
5. 所有业务阈值必须放在 Profile / Config 中，禁止硬编码。
6. 所有 AI 模型必须通过 Interface / Adapter 抽象。
7. 所有 Object Storage 必须通过统一 Adapter。
8. Pipeline Stage 必须独立。
9. Job 必须支持状态追踪。
10. 每个 Image 必须有独立状态。
11. 失败必须区分系统失败和业务 Reject。
12. 所有 Worker Task 必须具备 Retry / Timeout。
13. 所有耗时阶段必须记录 duration_ms。
14. 所有日志必须使用 Structured Logging。
15. 禁止在生产代码中使用 print。
16. API 必须生成 OpenAPI。
17. 代码必须添加类型标注。
18. 新增核心模块必须添加测试。
19. 优先保证正确性和可维护性，再优化性能。
20. 性能优化必须通过 Benchmark 证明。
21. 禁止对所有图片直接运行高成本生成式 AI。
22. 优先使用 Thumbnail 进行筛选。
23. Reject 图片必须 Early Exit。
24. Beautify 只能默认处理 Selected 图片。
25. 不要擅自改变既定 API Schema。
```

---

# 89. Codex 第一次执行 Prompt

将本 Markdown 放入项目根目录后，可以给 Codex：

```text
请完整阅读《多图智能筛选与自动美化服务开发方案.md》。

现在开始开发这个项目。

要求：

1. 当前只执行 Phase 1。
2. 不要提前实现 Phase 2 及后续功能。
3. 按文档指定目录创建项目骨架。
4. 使用 Python 3.12、FastAPI、PostgreSQL、Redis、Celery、MinIO。
5. 提供 Docker Compose。
6. 提供 .env.example。
7. 配置 Alembic。
8. 实现 GET /health。
9. 编写最基础测试。
10. 启动并运行测试。
11. 最后输出：
   - 本阶段完成内容
   - 新增文件
   - 启动方式
   - 测试结果
   - 尚未完成内容
   - Phase 2 下一步建议

不要一次性把整个系统做完。
```

---

# 90. 后续 Codex 执行方式

每一个 Phase 完成后使用：

```text
继续阅读项目根目录中的开发方案。

检查上一阶段实际代码和测试状态。

如果上一阶段存在问题，先修复。

确认测试通过之后，只执行下一个 Phase。

不要跳阶段。

完成后运行所有相关测试，并输出：
1. 完成内容
2. 修改文件
3. API 变化
4. 数据库变化
5. 测试结果
6. 性能影响
7. 下一阶段建议
```

---

# 91. 最终产品目标

最终形成：

```text
用户上传 20~50 张图片
        ↓
系统自动检测质量
        ↓
自动淘汰明显差图
        ↓
自动识别重复图片
        ↓
按业务标准评分
        ↓
自动选出最佳图片
        ↓
自然美化
        ↓
数秒级返回结果
```

对于 App 来说，只需要完成：

```text
Upload
+
Create Job
+
Get Progress
+
Get Results
```

对于后台来说，则形成：

```text
Filter Engine
+
Rank Engine
+
Enhance Engine
+
Rule Engine
+
Inference Engine
+
Async Job System
```

这套架构可以继续扩展到：

```text
小红书内容选图
企业活动照片
PPT 照片筛选
商品图片
人物写真
无人机航拍
商户图片审核
用户 UGC 图片
AI 相册
```

而不需要重新开发整个底层系统。

---

# 92. 核心开发优先级

任何时候发生技术取舍时，按照以下顺序决策：

```text
1. 正确性
2. 处理速度
3. 稳定性
4. 自然美化效果
5. 可扩展性
6. GPU 成本
7. 高级 AI 能力
```

不要为了“AI 感”牺牲速度和稳定性。

第一版的核心不是：

> 把每张图片修得非常夸张。

而是：

> **快速、稳定、自动地从一批图片中挑出真正值得使用的图片，并把它们自然地优化好。**
