# 时间优化执行记录：P0 基线与接口核验

记录时间：2026-09-07 12:00（Asia/Shanghai）。

**状态：P0 部分完成，人工质量标注与同版本完整链路基线尚未通过；没有实施或发布 P1–P3 性能改动。** 不报告实际提速比例，不把测试通过等同于质量验收完成。

## 已完成与证据

所有私有证据位于 `artifacts/time-optimization/20260907/`，该目录被 Git 忽略；没有把素材、配置凭据或完整环境变量提交到仓库。

| 检查 | 结果 | 证据文件 |
| --- | --- | --- |
| 改动前后端完整测试 | 401 项通过，55.76 秒；1 项既有弃用警告 | baseline-tests.xml / .log |
| 接口、回调、限流、恢复等专项 | 58 项通过，5.14 秒；属于上述基线的子集，不能重复累计 | api-automation-tests.xml / .log |
| 前端基线 | 6 个文件、18 项通过，17.45 秒 | frontend-baseline-tests.log |
| 新增样本清单校验工具 | 10 项通过；校验文件身份、去重及审核字段完整性 | dataset-validator-tests.xml |
| 既有 Logo 样本回放 | 2 张通过，3 个框，无误检漏检，框外像素变化为 0，要求保留的文字未丢失 | logo-baseline/logo-sample-acceptance.json |
| 素材库冻结 | 保存 242 张素材的元数据、向量和 49 个分组的参考信息 | library-snapshot.jsonl |
| 候选原图 | 跨分组只读下载 140 张不同图片，逐张 SHA256 核对，无下载错误；连同本地样本共 155 条记录、154 张不同图片 | candidate-transfer-status.json |
| 生产版本 | 保存各服务实际镜像 ID、镜像引用、启动参数、资源限制及部分代码哈希 | server-baseline.log、production-code-hashes.log |
| 本地代码版本 | HEAD f1697e7f19e89ec324d51c74bbf76be9dab122b8；保存源码文件哈希清单 | local-source-manifest.json |

Logo 回放使用本地已有不可变镜像 `ghcr.io/zuixi01/tuxiangshibie-api@sha256:9d16a535234e9e4a0de84b2b0ec7bdc2f4781a9e0cbee5692a26c565687f5495`，网络隔离、单 CPU、内存 3 GiB。这是既有 mosaic 验收脚本的结果，不能替代生产 overlay 输出验收或 140 张质量对照。第一次直接运行脚本因模块路径失败；查明后改用模块方式运行通过，两次均无 OOM。未清理测试容器。

全仓 Ruff 基线有 2 处既有导入排序问题，位于 `src/services/images/cover_score.py` 和 `src/workers/processing.py`；本次没有顺手修改它们。新增脚本与测试的 Ruff 检查通过。

## 接口通畅与自动化运行

- 生产 `/health` 与 `/health/ready` 均 HTTP 200，单次测得约 48 ms、200 ms；就绪接口报告数据库结构、Redis、对象存储和配置正常。这是检查时点的结果，不是持续可用性证明。
- 生产处理 Worker、回调 Worker 正常运行，Celery Beat 运行中；现存 447 个带回调任务的状态均为 delivered，最多投递 1 次。该统计没有区分任务业务成功与失败，不能据此认定图片处理全部成功。
- 运行配置：回调最多投递 3 次、单次超时 15 秒、重试基数 5 秒、补偿扫描 5 秒、投递租约 120 秒。已修正 `docs/company-api.md` 中默认 5 次及回调 Worker 的过时说明，并补充 429 与响应丢失处理要求。
- API 专项测试使用替身依赖，不是真实创建任务至 AI、结果查询、回调的完整验证。
- 本地现有环境 `/health` 为 200，但 `/health/ready` 为 503，原因是 database_schema 不就绪；没有直接迁移或覆盖已有本地数据库。隔离基线环境仍需完成，不把该环境作为可用基线。
- 当前没有跨创建请求的 Idempotency-Key 保证；响应丢失后的无人值守恢复尚未完成验证。客户端必须持久化已取得的 job_id，并避免无限重发创建请求。服务端跨请求幂等属于后续独立兼容性改动，未擅自加入本轮性能实现。

接口证据：`api-health.json`、`api-automation-runtime.log`。按用户要求，未开展上游鉴权修复。

## 资源与版本限制

只读服务器检查覆盖 uptime、CPU、内存、swap、磁盘、inode、Docker 占用、Compose 状态及近期内核异常。检查时 8 CPU、5 分钟负载约 0.72，目标磁盘约 76%、剩余约 25 GiB，inode 约 19%，未见最近两小时 OOM 等异常。后续一次 MemAvailable 为 3,966,728 / 15,731,668 kB，约 25.22%；此前有约 24.9% 的观测，位于用户 25% 下限附近。

这不是发布许可。每次实际发布前必须重做用户 AGENTS.md 的资源检查；不得用旧快照推断届时资源足够。线上多个服务镜像与本地 HEAD 不一致，需先固定可复现的基线运行版本，不能直接将本地镜像输出当作线上等价输出。

本次启动了本地 Docker Desktop 用于隔离 Logo 回放，已有本地容器随 Docker 启动；没有执行远程构建、部署、清理、重启、数据库写入或生产配置修改。

## 尚缺的验收输入

执行计划第 3、4 节明确要求人工标注与成功基线可用后进入 P1。当前仅找到 2 张既有 Logo 标注，没有完整分类、过滤及匹配真值集。

候选浏览页：`artifacts/time-optimization/20260907/quality-review.html`；清单：`quality-review-manifest.json`。其中素材库标签明确标记为 reference，审核状态均为 pending。新增下载数量足够不等于覆盖足够；图库可能缺少应拒绝、错误文件等困难样本。

验收人需从候选中选择并补齐至少 140 张不同图片：明确分类 40、困难过滤 40、Logo 20、匹配边界 20、异常图片 20；填写正确结论、需保留区域、审核人和时间。重复原图不能双计，模型自身输出不能替代人工真值。确认前，清单校验输出 not_ready 是预期阻断结果。

完成标注后运行：

```powershell
.venv/Scripts/python.exe scripts/validate_time_optimization_dataset.py artifacts/time-optimization/20260907/quality-review-manifest.json --root . --output artifacts/time-optimization/20260907/dataset-gate.json
```

该命令只检查清单完整性和文件身份；通过仍需完成隔离环境的成功基线、确定性回放、真实质量对照与端到端回调验收。

## 后续顺序与完成定义

1. 获得人工审核答案，固定可复现的基线运行环境，完成真实任务及测试接收端的完整链路。
2. 按原计划逐项实施同输入向量复用、渲染回收实验、公平调度；每项独立质量及恢复验收，失败则不进入发布。
3. 容量验证通过后才考虑入口 3→4 张/分钟；生产观察至少 24 小时且有足量成功样本，再评估 5 档。

剩余工作尚未完成，尤其不能用本次基线测试声称批次 P95 已降低 10%、质量未退化或接口已实现所有异常场景的无人值守运行。
