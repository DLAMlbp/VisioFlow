# 接口与整体运行核验

时间：2026-09-07 14:20–14:23，Asia/Shanghai。方式：只读核验；未发布候选、修改线上配置、创建测试业务任务或发送测试回调。

**结论：接口访问、后台服务和失败回调正常；真实图片成功处理链路未通过验收，不能报告整体业务正常。**

| 检查 | 结果 |
| --- | --- |
| 外部健康接口 | `/health` HTTP 200，71 ms |
| 外部就绪接口 | `/health/ready` HTTP 200，147 ms；数据库结构、Redis、对象存储、配置检查通过 |
| 内部 API | 健康、就绪、现有任务进度、结果查询均 HTTP 200 |
| Nginx 对外 Host 路由 | 健康、就绪、现有任务进度、结果查询均 HTTP 200；进度/结果约 73/117 ms |
| 进程与容器 | Compose 所列 API、Web、数据库、Redis、对象存储与全部 Worker 运行且 healthy；Celery Beat 运行 |
| 最近两小时任务 | 36 个任务全部 failed，失败节点 classification_and_filtering，错误码 AI_CONFIGURATION_ERROR |
| 失败类型 | 36 条失败摘要均包含鉴权/认证失败类描述；未输出原始错误文本、密钥或图片地址 |
| 最近两小时回调 | 36 个任务全部 delivered，最多投递 1 次 |
| 未结束任务 | 查询时没有非终态任务；不能因此认定业务成功 |
| 成功记录 | 当前保留的数据中未查到 selected_count > 0 的 completed 任务；不代表历史上从未成功 |
| 新优化是否在线 | 运行代码尚无公平调度配置字段；本地候选未部署，不能把当前失败归因于候选改动 |

就绪检查当前验证“配置存在”和依赖可访问，不执行上游 AI 的真实成功调用。因此它返回 200，与图片处理因上游鉴权失败而终止并不矛盾。按照用户之前“不处理 401 问题”的范围，没有擅自修改鉴权配置；但整体业务验收必须包含一次真实成功处理，不能跳过该阻断后宣称正常。

服务器 8 CPU，5 分钟负载 0.83；目标文件系统占用 76%，剩余约 25 GiB，inode 占用 19%；最近两小时内核日志未发现 OOM、I/O/文件系统错误或 panic。MemAvailable 为总内存 **23.87%**，低于用户要求的 25% 下限。此状态不允许发布或扩并发；没有重启、清理或调整系统配置。

本地候选已有完整后端 428 项测试通过，其中 16 项使用真实 Redis；静态检查通过。它们是代码与调度组件验证，不能替代上述真实业务链路验收。

剩余必要条件：解决实际业务的上游鉴权阻断，完成创建任务→图片成功处理→结果查询→回调送达的真实成功验证；新优化另需质量对照、性能对照、镜像验证及发布前资源检查。当前只确认访问和自动任务终结/失败通知链路正常。
